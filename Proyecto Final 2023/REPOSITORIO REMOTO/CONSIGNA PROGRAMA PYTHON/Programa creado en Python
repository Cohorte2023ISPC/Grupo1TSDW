import mysql.connector

coneccion = mysql.connector.connect(     
            host='localhost',
            user='root',
            password='purosveintepesos2023',
            db = 'ProyectoFinal'
            )

#class Normativa():       
    def consultarNormativa(self):#USAR PARA MOSTRAR ES EL SELECT
        print("--------CONSULTAR NORMATIVAS----------")
        cursor= coneccion.cursor()
        cursor.execute("SELECT * FROM Normativa")
        varias_norm = cursor.fetchall()
        x = 0
        y = 0
        for bd in varias_norm:
            print(bd)
            y +=1   
    
    def ingresarNormativa(self):# linea 35 da error q base de datos no toma los parametros
        print("--------INGRESAR NORMATIVA----------")
        nomb_nuevo= input("ingrese el nuevo nombre \n")#cambiar
        edad_nueva= input("ingrese la edad \n")#cambiar
        cursor= coneccion.cursor()
        cursor.execute("INSERT INTO usuarios (nombre, edad) VALUES (?,?)", (nomb_nuevo,edad_nueva))
        cursor.commit()
        print(f"El usuario{nomb_nuevo} fue agregado exitosamente")
'''class BasedDatos():
   
    def consultarCategoria(self):
        if self.coneccion.is_connected():
            cursor=self.coneccion.cursor()
            cursor.execute("SELECT Categoria_Norma")
            resultado= cursor.fetchall()
            return resultado
       
def menuPrincipal():
    
    while True:
        print("---MENU PRINCIPAL---")
        print("1.- Consultar normas")
        print("2.- Consultar normas")
        print("3.- Consultar normas")
        print("4.- Consultar normas")
        print("5.- Salir")
        print("--------------------")
        opcion= int(input("Seleccione una opcion: "))
    
        if opcion < 1 or opcion > 5:
            print("Opcion incorrecta, ingrese nuevamente.")
        elif opcion == 5:
            print("¡Gracias por usar el sistema!")
            break
        else:
            ejecutarOpcion(opcion)
            
def ejecutarOpcion(opcion):
    dao = BasedDatos()
    
    if opcion ==1:
        dao.consultarNorma()
    elif opcion ==2:
    elif opcion == 3:
    else opcion == 4:
    
    
menuPrincipal()
'''